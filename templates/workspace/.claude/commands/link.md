---
description: 記事・YouTube URL を取り込んでセカンドブレインに保存
argument-hint: <URL>
---
Second Brain の /link コマンド。ユーザー入力: $ARGUMENTS

`second-brain/.claude/commands/link.md` を読み、その手順に従って実行する。
手順中の `brain` の読み替えとボルト内パス（`_config/` `_templates/` 等）の解釈は、
このワークスペースの `CLAUDE.md` を参照すること。
