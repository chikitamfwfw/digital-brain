---
description: ローカルボルトと GitHub を同期する
---
Second Brain の /sync コマンド。

`second-brain/.claude/commands/sync.md` を読み、その手順に従って実行する。
手順中の `brain` の読み替えは、このワークスペースの `CLAUDE.md` を参照すること
（`brain sync` を実行。競合時は処理を止め、競合ファイルを伝えて手動解決を依頼する）。
