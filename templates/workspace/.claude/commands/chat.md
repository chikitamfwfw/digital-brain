---
description: AI と自由に会話（任意でノートに保存）
argument-hint: <メッセージ>
---
Second Brain の /chat コマンド。ユーザー入力: $ARGUMENTS

`second-brain/.claude/commands/chat.md` を読み、その手順に従って実行する。
手順中の `brain` の読み替えとボルト内パス（`_config/` `_templates/` 等）の解釈は、
このワークスペースの `CLAUDE.md` を参照すること。
