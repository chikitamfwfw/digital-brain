---
description: メモを記録し、AIと会話してセカンドブレインに保存
argument-hint: <メモ内容>
---
Second Brain の /memo コマンド。ユーザー入力: $ARGUMENTS

`second-brain/.claude/commands/memo.md` を読み、その手順に従って実行する。
手順中の `brain` の読み替えとボルト内パス（`_config/` `_templates/` 等）の解釈は、
このワークスペースの `CLAUDE.md` を参照すること。
