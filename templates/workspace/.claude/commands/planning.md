---
description: 企画・目標を AI とブレストしてノート化
argument-hint: <テーマ>
---
Second Brain の /planning コマンド。ユーザー入力: $ARGUMENTS

`second-brain/.claude/commands/planning.md` を読み、その手順に従って実行する。
手順中の `brain` の読み替えとボルト内パス（`_config/` `_templates/` 等）の解釈は、
このワークスペースの `CLAUDE.md` を参照すること。
