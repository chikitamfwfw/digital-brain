---
description: ノートを走査して Permanent ノートの候補をテーマ付きで提案・抽出
---
Second Brain の /permanent コマンド。

`second-brain/.claude/commands/permanent.md` を読み、その手順に従って実行する。
手順中の `brain` の読み替えとボルト内パス（`_config/` `_templates/` 等）の解釈は、
このワークスペースの `CLAUDE.md` を参照すること。
